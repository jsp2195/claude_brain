model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then use `.claude/agents/coordinator.md` and `.claude/rules/*.md` as the authoritative playbooks.
Coordinate only; delegate implementation and verification.
"""
