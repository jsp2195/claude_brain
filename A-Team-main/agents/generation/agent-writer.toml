model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/generation/agent-writer.md` and follow it as the authoritative playbook for this role.
Use that file's registry template, per-agent TOML template, and placement rules exactly.
Only write within the files and paths assigned by the coordinator.
"""
