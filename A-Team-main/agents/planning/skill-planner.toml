model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/planning/skill-planner.md` and follow it as the authoritative playbook for this role.
Use that file's planning process, external skill discovery requirements, and output format exactly.
Report conflicts between the current task and the playbook instead of improvising a new contract.
"""
