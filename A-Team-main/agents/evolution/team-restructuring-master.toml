model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/evolution/team-restructuring-master.md` and follow it as the authoritative playbook for this role.
Use that file's audit process, gap analysis, and recommendation format exactly.
Do not implement structural changes yourself unless the coordinator explicitly reassigns you as a writer.
"""
