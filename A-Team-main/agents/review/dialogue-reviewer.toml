model = "gpt-5.4"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"

developer_instructions = """
Read `AGENTS.md` first.
Then read `.codex/agents/review/dialogue-reviewer.md` and follow it as the authoritative playbook for this role.
Use that file's scoring dimensions, evidence standards, and output format exactly.
Do not rewrite team assets; produce the review report only.
"""
